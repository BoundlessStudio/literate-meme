{
  "name": "database-marketplace-app",
  "version": "0.1.0",
  "description": "OpenAI Apps SDK sample that sells hosted databases and exposes SQL execution endpoints.",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env src/server.js",
    "start": "node src/server.js",
    "lint": "node scripts/lint.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.1",
    "sql.js": "^1.9.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "lint-staged": "^15.0.2"
  }
}
